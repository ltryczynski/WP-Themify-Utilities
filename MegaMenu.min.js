class MegaMenu { constructor({ menuElements: e, submenuSelector: t = "ul.sub-menu.tf_scrollbar", mobileBreakpoint: s = 786, enableMobile: i = !1 }) { this.menuElements = [...e], this.submenuSelector = t, this.mobile = { mobileBreakpoint: s, enableMobile: i }, this.setUp() } setUp() { let { enableMobile: e, mobileBreakpoint: t } = this.mobile; (!0 === e || window.innerWidth > t) && this.setWidth() } setWidth() { let { menuElements: e, submenuSelector: t } = this; for (let s in e) e[s].querySelector(t).style.width = `${e[s].clientWidth}px` } }
new MegaMenu({ menuElements: document.querySelectorAll(".main-nav") });